/* To learn more about Typescript configuration file: https://www.typescriptlang.org/docs/handbook/tsconfig-json.html. */
/* To learn more about Angular compiler options: https://angular.dev/reference/configs/angular-compiler-options. */
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/spec",
    "rootDir": ".",
    "types": [
      "vitest/globals",
      "node"
    ],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": [
    "src/**/*.d.ts",
    "src/**/*.spec.ts",
    "tools/**/*.ts"
  ],
  /* tools/google3d is a one-shot job with its own package.json, dependencies and
     Google Maps types; it never runs with the game or its tests. */
  "exclude": [
    "tools/google3d/**/*.ts"
  ]
}
